# ソークテスト監視・ハーネス改修まとめ（2026-06-13）

長期運用テストの監視セッションで実施した調査・修正・運用方針のまとめ。
実行環境: テスト拡張(EggCameraTestExt)は **Windowsタブレット** のChrome、サーバ(:3000)とリポジトリは **Mac mini**(192.168.10.104)。

## TL;DR — 有効化チェックリスト（予定: 06-13 13:00 JST頃）

- [ ] タブレットで `http://192.168.10.104:3000/EggCameraTestExt.zip` をDL（19.9KB）
- [ ] 展開して拡張フォルダを置き換え（変更は content.js / hook.js のみ。manifest変更なし）
- [ ] chrome://extensions で拡張をリロード → テストタブをリロード（テストは自動再開）
- [ ] ついでにタブレットの ダウンロード/EggCameraTest/ の失敗スクショを掃除
- [ ] Mac mini でサーバ再起動（chaos.js の発火履歴と .preview トリムの有効化）
- [ ] 再起動したら `/api/test-report` に DEPLOY-MARKER を一言投げておくと監視が誤検知しない

zipは `EggCameraUserUI/dist/` に置いてあるため、**UIを再ビルドすると消える**（必要なら `zip -r EggCameraTestExt.zip EggCameraTestExt` で再生成）。

## 修正一覧

### テストハーネスの偽陽性つぶし（content.js / hook.js / src/chaos.js）

監視で見つかった誤報3系統の対策。アプリ本体のバグは一件もなかった。

| # | 問題 | 修正 |
|---|---|---|
| 1 | 統計が「注入0」のまま — `bumpStat` の get→set が並行すると後勝ちで加算消失 | Promiseチェーンで直列化 |
| 2 | 多重注入でクライアント側フォルトが先に発火すると、サーバ側CHAOS(r2/qr)が未消費で残留 → 後のサイクルで「▲ 注入なしでオーバーレイ」誤報 | FAULTSに `group`(capture/composite)を導入し、同groupの同時注入を打ち切り |
| 3 | 同上の残留対策 | サイクル開始時 `clearLeftoverFaults()` がhookをclear＋armed>0ならCHAOSをDELETE（ログ「残留サーバフォルトを掃除」） |
| 4 | セッション事前作成のタイミングずれで session-create 注入が踏まれず「▲ 想定外」誤報 | 発火追跡(`firedFaultIds`)を導入。未発火は「△ 注入未発火のままサイクル完了」（持ち越し・カウンタ加算なし）、発火したのにオーバーレイなしの時だけ▲ |
| 5 | 「注入なしでオーバーレイ」を即実バグ断定 | `verifyUnexpectedOverlay()` がCHAOSの armed残数＋直近発火履歴(`recentFired`、chaos.jsに追加)を照会してから判定 |

### ストレージリーク修正（src/composite.js）

`data/raw/.preview`（HEIC→JPEG変換のプレビュー置き場）に掃除がなく **2.1GB/611ファイル** 蓄積していた。
合成保存後のトリムに `trimLocalDir(PREVIEW_DIR, MAX_COMPOSITED)` を追加（新しい30件を残して削除）。
溜まっていた分は 06-12 に手動掃除済み（2.1GB→102MB）。**コードの有効化はサーバ再起動後**。

### 人間の「意外な行動」注入 — quirks（content.js）

実運用は子供や家族が操作する前提で、整然としたフロー以外への耐性を検証する。
注入なしサイクルの **15%** で1つ実行。フォルト注入とは同時に走らない。ログは `◇` プレフィックス（alertではない）。

| quirk | 内容 | 重み |
|---|---|---|
| shutter-mash | シャッター130ms間隔6連打。「◇ 連打の結果: N/6 回が有効なボタンに着弾」で連打ガードの有無が分かる | 2倍 |
| next-mash | 名前入力後「次へ」5連打（二重遷移の確認） | 2倍 |
| double-click | 保存ボタン二度押し（二重送信の確認） | 2倍 |
| impatient-tap | アップロード中にボタン連打（せっかち操作） | 2倍 |
| reload-midflow | フロー途中でブラウザリロード（誤操作からの復帰） | 1 |
| idle-pause | 入力画面で75秒放置（アイドルリセットの挙動を観察してログ） | 1 |
| rapid-reselect | 写真選択を素早く5回切り替え | 1 |

連打系を2倍にしたのは「ボタン連打はありがち」のため。統計行に「行動N」カウンタを追加済み。

## 新ログの読み方（更新後に出始める）

- `残留サーバフォルトを掃除 (capture:N r2:N qr:N)` — 正常（持ち越し分の後始末）
- `△ 注入未発火のままサイクル完了` — 正常（タイミングずれの持ち越し、次サイクルで掃除）
- `△ 注入なしでオーバーレイ表示 — 残留サーバフォルトが原因の可能性` — 正常（誤報の格下げ）
- `◇ 意外な行動: …` / `◇ 連打の結果: …` — 正常（quirksの実行記録）
- `◇ 連打の結果: 6/6 回が有効なボタンに着弾` — **要注目**: アプリ側の連打ガード不足のサイン
- `▲` で始まる行 — 引き続き本物の要注意

## リソース棚卸し（06-12時点）

| 場所 | 状態 | 上限の仕組み |
|---|---|---|
| Mac mini ディスク | 問題なし | composited/raw/.preview 各30件、ログはメモリ2000行＋日次~110KB |
| Mac mini 負荷 | rss 100-270MB / load ~1.3 | — |
| R2 | ピーク~2.5GB | 24h保持、起動時＋3hごと削除（server.js:75-76） |
| 通信量 | 上下計 ~4.5GB/日 | 1サイクル≒UP2.5MB＋DL検証2.5MB × ~900サイクル/日 |
| タブレット | DL検証はメモリ上のみ | 失敗スクショだけ手動掃除が必要 |
| iPhone | LAN内の単発フレームGET方式（常時ストリームなし） | **熱・バッテリー劣化は要注意**。サイクル所要時間の漸増を代理指標に監視 |
| 無限ループ | 未防護経路なし | ポーリング20回打ち切り / リトライストーム監視(composite4・capture8・session5) / 停滞150s・オーバーレイ30sで強制リロード |

## 監視体制

- 30分ごと（毎時 :06/:36）に `node tools/check-soak.js 35` を実行し要注意ログを調査・報告
- あわせて `node tools/check-r2-cost.js`（新設・読み取り専用）でR2の残量/最古オブジェクト/無料枠を確認。wranglerはMac mini未認証のため、正確な操作数が必要なら `wrangler login` が必要
- 異常ログに加えて毎回確認: data/配下のサイズ増、サイクル所要時間の漸増（iPhone熱）、コスト警告・ポーリング暴走（通信量）、rss/load の漸増
- **拡張更新までの既知の偽陽性**（旧コード由来、報告対象外）: 統計の「注入0」「想定外1」、多重注入後の「▲ 注入なしでオーバーレイ」、未発火サイクルの「▲ 想定外」

## 経緯メモ

- 06-12 14:30前後の「▲ 注入なし」2件＝composite-upload＋r2/qr三重注入の残留が原因（実バグではない）
- 06-12 15:06の「▲ 想定外」＝session-create事前作成とのタイミングずれ（実バグではない）
- Cloudflare障害（06-12夜、Dashboard/API系インシデント）はR2/Pagesのデータプレーンに波及せず、EggCameraへの実影響ゼロを確認済み
