# Mac mini 再起動後 自動復旧レポート(2026-06-21)

- 対象: Mac mini(EggCamera サーバ／Appium／iPad 運用テスト基盤)
- 再起動: 2026-06-21 15:01 JST(週次ゲート経由)
- 本レポート作成: 2026-06-21 22:11 JST
- 実行: post-boot-claude(自動復旧確認エージェント)

---

## サマリ

| 区分 | 結果 |
|---|---|
| (A) サービス復旧 | ✅ 全正常 |
| (B) リフレッシュ前後比較 | ✅ 効果確認 |
| (C) カメラ復帰 | ✅ 200 |
| (D) post-boot 運用テスト(15:04) | ✅ 完走 5/6(失敗1は既知の自動回復) |
| **追加発見: 毎時テストの障害** | 🔴 **18時以降劣化→20〜22時 全滅(レート制限)** |

> post-boot の自動復旧そのものは正常完了。ただし**その後 18 時以降に進行した別障害**を、ユーザー指摘を機に発見した。

---

## (A) サービス復旧 — 全正常

| 項目 | 結果 |
|---|---|
| node `:3000/` | 200 OK |
| admin `:3001/` | 応答あり(ルート404・プロセス稼働) |
| appium `:4723/status` | 200 OK |
| cloudflared | 稼働・トンネル登録済(connIndex 1-3, 06:01:46Z) |
| node プロセス | PID 1046, 15:01:51 起動から連続稼働(再起動なし) |

## (B) リフレッシュ前後比較 — 効果確認

| 指標 | 再起動前[pre] | 現在 | 判定 |
|---|---|---|---|
| node RSS | 111.2 MB | 87.6 MB | 低下 ✅ |
| iproxy.out | 364,544 B | 0 B | 0化 ✅ |
| appium.out | 2,236,481 B | 2,183 B | ほぼ0化 ✅ |
| /tmp/appium.log | 2,109,247 B | 1,953 B | ほぼ0化 ✅ |
| disk free | 373.6 GB | 374.7 GB | +約1.0 GB ✅ |

## (C) カメラ復帰 — `:8080/frame` 200 OK

## (D) post-boot 運用テスト(15:04 実行)— 完走 5/6

- iPad UDID `00008132-...401C` を Appium 公式トンネル在席(USB)確認後に実行。
- 結果: **完了5 / フォルトOK0 / スキップ0 / 失敗1**(exit 0)。
- 失敗1 = C1(再起動直後の初回セッション)で keepalive 連続失敗 → セッション自動再生成で回復。C2〜C6 連続成功・カスケード残留なし。**既知・想定内の自動回復パターン**。
- DEPLOY-MARKER を test-report / Slack に `info` で投稿済み。

---

## 🔴 追加発見: 毎時テストの全滅障害(レート制限)

### 事象
毎時テスト(`com.eggcamera.hourly-test`)が時間経過とともに劣化し、20〜22時枠で全6サイクル全滅。
watchdog(`com.eggcamera.hourly-test-watchdog`, 毎時50分)は**正常稼働**しており、これを ALERT で検知済み。

| テスト枠(JST) | 結果 | rate-limit 件数 |
|---|---|---|
| 17:00 | 完了6 / 失敗0 | — |
| 18:00 | 完了4 / 失敗1 | — |
| 19:00 | 完了0 / 失敗5 | 62 |
| 20:00 | 完了0 / 失敗6(全滅) | 76 |
| 21:00 | 完了0 / 失敗6(全滅) | 60 |
| 22:00 | 完了0 / 失敗6(全滅) | 67 |

### 症状
- 撮影クリック直後に **「ご迷惑をおかけして申し訳ありません。お近くのスタッフにお声掛けください」** という致命的エラー時フォールバック画面(全画面オーバーレイ)を表示。
- テストは「想定外オーバーレイ」として各サイクル失敗。

### 真因
- サーバログ(`eggcamera-node.err`)に `rate-limit: public ::ffff:192.168.10.112 /preview/frame`(/mode, /sessions, /events, /client-log)が**大量連続**。
- レート制限設定: `server.js:38` `publicLimiter` = **windowMs 60s / max 200**(IP単位, 超過は 429)。
- テストiPad(192.168.10.112)の `/preview/frame` 等のポーリングバーストが 200/分を超過 → 429 → アプリがエラー画面 → クライアントがリトライ → さらにリクエスト増、の**正のフィードバック悪循環**で全滅まで悪化。
- **現在(22:02 以降)rate-limit は停止**=テスト非実行時のサーバ自体は健全。本番影響はレート制限が IP 単位のため、当該テストiPad に限定されると考えられる(要確認)。

### 切り分けの要点
- node/admin/appium/camera/トンネルは全て生存。リソース枯渇なし(ディスク348GB空・Load 1.1・合成ゾンビなし・スクショ30MB)。
- 17時まで完走していたため、再起動起因ではなく **18時以降に顕在化したポーリング過剰 × レート制限のミスマッチ**。

---

## 推奨アクション(未実施・要判断)

1. **レート制限の見直し**: `/preview/frame` を publicLimiter から除外、または max を引き上げ(プレビューは高頻度ポーリング前提のため)。
2. **クライアントのプレビューポーリング頻度の確認・抑制**(200/分に張り付く設計はリトライ増に脆弱)。
3. **エラー画面後のリトライ抑制**(悪循環の遮断)。
4. **post-boot 手順の改善**: 復旧確認(A〜E)に「直近の毎時テスト watchdog 結果の確認」を追加(今回はユーザー指摘で発覚。手順に組み込めば自動で気づける)。

---

## 備考: ユーザー指摘「毎時50分の監視が動いていない」への回答
- watchdog 自体は **launchd 登録済み・毎時50分に発火・ALERT 検知も正常**(動いている)。
- ただし post-boot-claude の手順に「毎時テストの健全性確認」が無く、復旧確認だけでは本障害を見落とす状態だった。指摘は**手順の穴**として的中。
